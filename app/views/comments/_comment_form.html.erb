<%= form_for(@add_comment, url: post_comments_path) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "コメントを入力してください" %>
  </div>
  <%= f.submit "投稿する", class: "btn btn-primary" %>
<% end %>